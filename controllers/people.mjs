export default function initPeopleController(db) {
  // For now, people will have no controllers
  // creation of new people can be done at booking
  const index = (request, response) => {
    //
  };

  return {
    index,
  };
}
